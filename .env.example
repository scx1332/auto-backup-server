# Generate a secret with: openssl rand -hex 32
BACKUP_TOKEN=change-me
PORT=3000
# Storage root; each project is stored in its own UUID subdirectory.
# Docker maps this host path to /data/backups.
BACKUP_DIR=./backups
